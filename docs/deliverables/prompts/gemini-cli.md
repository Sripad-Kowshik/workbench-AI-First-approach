# Sample prompt for Gemini CLI

Adapt the CHF harness using the bundle in this directory.

Follow the docs and templates exactly. Generate:
- `agents/hotfix_orchestrator.exs`
- deterministic Python scripts under `scripts/`
- the supporting audit and handoff artifacts described in the docs

Do not ask the model to invent file contents procedurally; keep generation deterministic.
